import 'package:flutter/material.dart';

class ActionMenuView extends StatelessWidget {
  const ActionMenuView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}