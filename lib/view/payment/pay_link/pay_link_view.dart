import 'package:flutter/material.dart';

class PayLinkView extends StatelessWidget {
  const PayLinkView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}