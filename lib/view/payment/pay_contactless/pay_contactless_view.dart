import 'package:flutter/material.dart';

class PayContactlessView extends StatelessWidget {
  const PayContactlessView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}