import 'package:flutter/material.dart';

class GetMoneyView extends StatelessWidget {
  const GetMoneyView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}